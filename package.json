{
  "name": "afl-performance-lab",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-positions.mjs && node scripts/build-data.mjs && node scripts/build-news.mjs",
    "build:data": "node scripts/build-data.mjs",
    "build:native": "npm run build && node scripts/build-native.mjs",
    "build:news": "node scripts/build-news.mjs",
    "build:positions": "node scripts/build-positions.mjs",
    "ios:add": "npm run build:native && npx cap add ios",
    "ios:open": "npm run build:native && npx cap sync ios && npx cap open ios",
    "ios:sync": "npm run build:native && npx cap sync ios",
    "start": "python3 -m http.server 4173 --bind 127.0.0.1"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
